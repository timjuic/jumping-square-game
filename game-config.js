const config = {
   ASPECT_RATIO: 16 / 9,
   BLOCK_SIZE: 50,
   GAME_SPEED: 2,
   GRAVITY: 0.5,
   JUMP_VELOCITY_Y: 10,
   JUMP_VELOCITY_SPIN: 15,
}

config.PLAYER_POSITION_X = config.BLOCK_SIZE * 2

export default config